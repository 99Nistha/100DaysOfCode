a^a=0
a^0=a
so all the same values will be cancelled out with each other and we will get our unique value because a^0=a, thats why we initialized tha val as 0
```
class Solution {
    public int singleNumber(int[] nums) {
        int val=0;
        for(int i=0;i<nums.length;i++){
            val^=nums[i];
        }
        return val;
    }
}
```
